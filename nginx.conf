server {
    listen 80;
    listen [::]:80;
    
    # REPLACE THIS WITH YOUR ACTUAL DOMAIN
    server_name unionglen.condosunion.com;
    
    root /var/www/unionglen;
    index index.html index.htm;
    
    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    
    # Gzip compression
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_proxied expired no-cache no-store private must-revalidate;
    gzip_types text/plain text/css text/xml text/javascript application/javascript application/xml+rss application/json;
    
    # Main location
    location / {
        try_files $uri $uri/ /index.html;
    }
    
    # Cache static assets
    location ~* \.(jpg|jpeg|png|gif|ico|css|js|svg)$ {
        expires 30d;
        add_header Cache-Control "public, immutable";
    }
    
    # Handle 404
    error_page 404 /index.html;
    
    # Logging
    access_log /var/log/nginx/unionglen-access.log;
    error_log /var/log/nginx/unionglen-error.log;
}

# HTTPS server (certbot will add this automatically)
# After running certbot, this section will be populated with SSL configuration
